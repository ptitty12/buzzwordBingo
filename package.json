{
  "name": "jargon-watch",
  "version": "1.0.0",
  "private": true,
  "description": "Jargon Watch — real-time, transcript-driven jargon tracking for meetings that could have been an email.",
  "license": "MIT",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c green,magenta \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "cd server && .venv/bin/python -m app.main",
    "dev:web": "npm run dev --workspace web",
    "build": "npm run build --workspace web",
    "test": "cd server && .venv/bin/python -m pytest -q",
    "typecheck": "npm run typecheck --workspace web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
